install the submodules in the node_modules directory:

>npm install -i

run the dev server with your app on port 8080

>npm run watch

package together all javascript into one assets/bundle.js file

>npm run build

finally build the go thing (needs bundle.js)

>go build
